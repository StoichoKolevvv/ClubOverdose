@model ClubOverdose.Data.Menu

@{
    ViewData["Title"] = "Delete Menu";
}

<section class="page-header">
    <div class="container">
        <h1>Delete Menu</h1>
        <p>Review this menu category before removing it.</p>
    </div>
</section>

<div class="container pb-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="form-card">
                <h3 class="mb-4 text-danger">Delete Confirmation</h3>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.Name)</span>
                    <span class="drink-value">@Html.DisplayFor(model => model.Name)</span>
                </div>

                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex gap-2 flex-wrap">
                        <input type="submit" value="Delete" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-outline-light">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

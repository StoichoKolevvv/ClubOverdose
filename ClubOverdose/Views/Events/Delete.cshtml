@model ClubOverdose.Data.Event

@{
    ViewData["Title"] = "Delete Event";
}

<section class="page-header">
    <div class="container">
        <h1>Delete Event</h1>
        <p>Review this event before removing it.</p>
    </div>
</section>

<div class="container pb-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="form-card">
                <h3 class="mb-4 text-danger">Delete Confirmation</h3>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.Name)</span>
                    <span class="drink-value">@Html.DisplayFor(model => model.Name)</span>
                </div>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.ImageUrl)</span>
                    <span class="drink-value text-break">@Html.DisplayFor(model => model.ImageUrl)</span>
                </div>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.Description)</span>
                    <span class="drink-value text-break">@Html.DisplayFor(model => model.Description)</span>
                </div>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.EventDateTime)</span>
                    <span class="drink-value">@Html.DisplayFor(model => model.EventDateTime)</span>
                </div>

                <div class="drink-detail-row">
                    <span class="drink-label">@Html.DisplayNameFor(model => model.LastUpdatedDate)</span>
                    <span class="drink-value">@Html.DisplayFor(model => model.LastUpdatedDate)</span>
                </div>

                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex gap-2 flex-wrap">
                        <input type="submit" value="Delete" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-outline-light">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
